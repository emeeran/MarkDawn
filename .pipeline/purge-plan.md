# Phase 1 — Purge Plan (dry run)

| path | reason | confidence | inbound-refs |
| --- | --- | --- | --- |
| `src-tauri/gen/schemas/*.json` (4 files) | generated by tauri-build on every compile; build artifact accidentally committed | high | none (regenerated) |
| `e2e/smoke.spec.ts` | **keep** — unrunnable today (needs tauri-driver) but intentional, documented in README §Tests | — | README |
| `PLAN.md` | **keep** — project design history, referenced by team convention | — | — |

No duplicate files, editor backups, or orphaned modules found (75 tracked files reviewed via inventory + reference grep).

**Apply action:** `git rm --cached src-tauri/gen/schemas/*.json` + add `src-tauri/gen/` to `.gitignore`
(regenerable build output — the one class where removal beats `trash2review`).
